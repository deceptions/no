<LinearLayout
        xmlns:android="http://schemas.android.com/apk/res/android"
        android:layout_width="match_parent"
        android:layout_height="match_parent" xmlns:app="http://schemas.android.com/apk/res-auto"
        android:gravity="center"
        android:orientation="vertical"
        android:padding="20dp"
        android:background="?attr/backgroundColor"

>
    <LinearLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:gravity="center"
    >
        <com.google.android.material.textfield.TextInputEditText
                android:id="@+id/number_picker_episode_text"
                android:layout_width="200dp"
                android:layout_height="wrap_content"
                android:gravity="center"
                android:inputType="number"
        />
        <LinearLayout
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:orientation="vertical"
        >

            <ImageView
                    android:layout_width="40dp"
                    android:layout_height="40dp"
                    android:id="@+id/number_picker_episode_up"
                    app:srcCompat="@drawable/ic_baseline_keyboard_arrow_up_24"
            >
            </ImageView>
            <ImageView
                    android:layout_width="40dp"
                    android:layout_height="40dp"
                    android:id="@+id/number_picker_episode_down"
                    app:srcCompat="@drawable/ic_baseline_keyboard_arrow_down_24"
            >
            </ImageView>
        </LinearLayout>
    </LinearLayout>

    <com.google.android.material.button.MaterialButton
            android:layout_margin="20dp"
            android:clipToPadding="false"
            android:clipChildren="false"
            android:layout_width="wrap_content"
            android:elevation="10dp"
            android:layout_height="40dp"
            android:background="@drawable/button_ripple"
            android:textColor="@android:color/primary_text_dark"
            android:foreground="@drawable/button_outline"
            android:theme="@style/Button.Black"
            android:id="@+id/episode_progress_btt"
            android:text="@string/number_picker_set_progress"
    />

</LinearLayout>